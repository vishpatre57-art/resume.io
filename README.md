
# Vishal Patre — Resume Website

A modern, responsive, single-page resume website built with **HTML, CSS, and vanilla JS**. Includes dark/light theme toggle, animated section reveals, SEO metadata, and JSON-LD structured data.

## 🚀 Quick Start (GitHub Pages)

1. Create a new repository on GitHub (e.g., `vishal-resume`).
2. Upload the files in this folder (`index.html`, `styles.css`, `script.js`, `assets/*`).
3. Go to **Settings → Pages**.
   - **Source**: Select `Deploy from a branch`.
   - **Branch**: `main` (root).
4. Save — your site will be available at `https://<your-username>.github.io/vishal-resume/`.

## 🛠 Customization

- Update text in `index.html` with your latest details.
- Replace `assets/og-cover.png` and `assets/favicon.svg` with your own branding.
- If you have a PDF resume, place it at `assets/Vishal_Patre_QA_Resume.pdf` so the Download button works.

## 📁 Project Structure
